---
description: Orchestrates an end-to-end feature lifecycle linking spec-split-plan, spec-generator, sql-schema-planner, dotnet-angular-architect, and spec-executor.
---

1. Activate `spec-split-plan` skill to decompose complex requirements into modular specs.
2. Activate `spec-generator` skill to generate structured specification files under `specs/`.
3. Activate `sql-schema-planner` skill if database entities or DDL modifications are required.
4. Activate `dotnet-angular-architect` skill to verify layer consistency across C# Minimal API and Angular.
5. Activate `spec-executor` skill to implement the planned changes step by step.
