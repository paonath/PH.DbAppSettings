# {SolutionName} — Solution Blueprint

> Generated by `solution-architect` skill on {date}.
> Update this file when entities are added or technology decisions change.

---

## 1. Solution Summary

| Key | Value |
|-----|-------|
| SolutionName | `{SolutionName}` |
| .NET version | `{dotnet-version}` |
| Angular version | `{angular-version}` |
| Database | `{database-engine}` |
| ORM | `{orm-strategy}` |
| Auth | `{auth-provider}` |
| Blueprint generated | `{date}` |

### Entities

| Entity | Table name | Description |
|--------|-----------|-------------|
| `{Entity}` | `{table_name}` | _{description}_ |

---

## 2. Resolved Project Map

| Project | Namespace | csproj file |
|---------|-----------|-------------|
| `{SolutionName}.Api` | `{SolutionName}.Api` | `{SolutionName}.Api.csproj` |
| `{SolutionName}.Dal` | `{SolutionName}.Dal` | `{SolutionName}.Dal.csproj` |
| `{SolutionName}.Models` | `{SolutionName}.Models` | `{SolutionName}.Models.csproj` |
| `{SolutionName}.Services` | `{SolutionName}.Services` | `{SolutionName}.Services.csproj` |
| `{SolutionName}.Services.Components` | `{SolutionName}.Services.Components` | `{SolutionName}.Services.Components.csproj` |
| `{SolutionName}Common` | `{SolutionName}Common` | `{SolutionName}Common.csproj` |

Solution file: `server/{SolutionName}.Api/{SolutionName}.sln`

---

## 3. Dependency Diagram

```
┌──────────────────────────────────────────┐
│           {SolutionName}.Api             │
└─────────────────┬────────────────────────┘
                  │
    ┌─────────────▼────────────────────────┐
    │  {SolutionName}.Services.Components  │
    └──────┬──────────────┬────────────────┘
           │              │
    ┌──────▼──────┐  ┌────▼──────┐
    │  .Services  │  │   .Dal    │
    └──────┬──────┘  └───────────┘
           │
    ┌──────▼──────┐
    │   .Models   │
    └─────────────┘

    ┌─────────────────────────────────────────────┐
    │  {SolutionName}Common   (no dependencies)   │
    │  optional dependency for ALL projects above │
    └─────────────────────────────────────────────┘
```

---

## 4. AGENTS.md Files

Paste the content of each block below into the corresponding `AGENTS.md` file.

---

### AGENTS.md (repo root)

```markdown
# {SolutionName} — Agent Guide

## Solution Overview
{one-paragraph description of the solution purpose}

## Architecture reference
- Blueprint: `specs/{solution-name}-blueprint.md`
- Template: `.agents/templates/solution-architecture-template.md`

## Cross-cutting rules
- Schema changes → numbered SQL script in `diff/` (no EF migrations)
- Specs in `specs/`; completed specs in `specs/implemented/`
- Never: duplicate entity classes between projects; add business logic to Api layer
```

---

### {SolutionName}.Api/AGENTS.md

```markdown
# {SolutionName}.Api — Agent Guide

## Purpose
Minimal API entry point. Routes HTTP, registers DI, configures middleware and authentication.

## Belongs here
- `Program.cs` — DI registration, middleware pipeline, MapGroup calls
- `ServiceRegistration.cs` — scoped service + validator registration
- `GlobalUsings.cs`
- `Endpoints/{Entity}/{Entity}Endpoints.cs` (entities: {list all entities})
- `Endpoints/EndpointMap.cs`

## Does NOT belong here
- Business logic → {SolutionName}.Services.Components
- Database access → {SolutionName}.Dal
- DTO definitions → {SolutionName}.Models
- Validation rules → {SolutionName}.Services.Components

## Conventions
- Endpoint handlers: private static async methods
- Authorization: .RequireAuthorization() at group level, never omitted
- No manual validation: inject IValidator<T> from DI only

## Dependencies
{SolutionName}.Dal, {SolutionName}.Models, {SolutionName}.Services,
{SolutionName}.Services.Components, {SolutionName}Common (optional)
```

---

### {SolutionName}.Dal/AGENTS.md

```markdown
# {SolutionName}.Dal — Agent Guide

## Purpose
Data-access layer. Owns all EF entity classes and DbContext.

## Belongs here
- `Context.cs` with DbSet<T> for: {list all entities}
- `IUnitOfWork.cs` + `UnitOfWork.cs`
- `DapperBase.cs`
- EF entity classes (one per DB table): {list all entities}

## Does NOT belong here
- DTOs → {SolutionName}.Models
- Business logic → {SolutionName}.Services.Components

## Conventions
- Entity classes never exposed outside this project
- New entity → new DbSet<T> in Context.cs
- Schema change → new numbered SQL script in diff/ (no EF migrations)

## Dependencies
{SolutionName}Common (optional)
```

---

### {SolutionName}.Models/AGENTS.md

```markdown
# {SolutionName}.Models — Agent Guide

## Purpose
DTO layer. C# record types for API input/output only. No EF entity classes.

## Belongs here
Read DTOs: {list all EntityDto}
Write DTOs: {list all EntityCreateDto}
One folder per entity: {Entity}/{Entity}Dto.cs + {Entity}CreateDto.cs

## Does NOT belong here
- EF entity classes → {SolutionName}.Dal
- Business logic → {SolutionName}.Services.Components

## Conventions
- All records use init properties
- {Entity}Dto adds only Id on top of {Entity}CreateDto
- No mapping logic inside records

## Dependencies
{SolutionName}Common (optional)
```

---

### {SolutionName}.Services/AGENTS.md

```markdown
# {SolutionName}.Services — Agent Guide

## Purpose
Contract layer. C# interfaces ONLY — zero implementation code.

## Belongs here
Interfaces: {list all IEntityService}
Standard CRUD contract per entity: GetAllAsync, GetByIdAsync,
CreateAsync, UpdateAsync, DeleteAsync

## Does NOT belong here
- Implementations → {SolutionName}.Services.Components
- Any concrete class

## Conventions
- Every method is async (Task / Task<T>)
- Methods operate only on Dto types, never on EF entity classes

## Dependencies
{SolutionName}.Models, {SolutionName}Common (optional)
```

---

### {SolutionName}.Services.Components/AGENTS.md

```markdown
# {SolutionName}.Services.Components — Agent Guide

## Purpose
Implementation layer. Service implementations + FluentValidation validators.

## Belongs here
Implementations: {list all EntityService}
Validators: {list all EntityValidator}

## Does NOT belong here
- Interfaces → {SolutionName}.Services
- Endpoint routing → {SolutionName}.Api

## Conventions
- Every EntityService takes IUnitOfWork + ILogger<T> via constructor injection
- Never bypass IUnitOfWork for DB access
- Every new service + validator must be registered in ServiceRegistration.cs
- No ad-hoc validation in methods — use EntityValidator only

## Dependencies
{SolutionName}.Models, {SolutionName}.Services, {SolutionName}.Dal,
{SolutionName}Common (optional)
```

---

### {SolutionName}Common/AGENTS.md

```markdown
# {SolutionName}Common — Agent Guide

## Purpose
Cross-cutting utilities. Constants, base classes, extension methods.

## Belongs here
- Constants/enums used by more than one layer
- Base classes with no layer-specific dependencies
- Extension methods with no layer-specific dependencies

## Does NOT belong here
- Layer-specific logic (any of Api, Dal, Models, Services, Services.Components)

## Conventions
- This project depends on NOTHING — no references to other solution projects

## Dependencies
none
```

---

## 5. Per-Entity Implementation Checklist

_Repeat the table below once per entity. Check off items as they are created._

### Entity: `{Entity}`

| # | Artifact | File | Done |
|---|----------|------|------|
| 1 | Schema script | `diff/{N} - Add{Entity}Table.sql` | ☐ |
| 2 | EF entity class | `{SolutionName}.Dal/{Entity}.cs` | ☐ |
| 3 | DbSet registration | `{SolutionName}.Dal/Context.cs` | ☐ |
| 4 | Read DTO | `{SolutionName}.Models/{Entity}/{Entity}Dto.cs` | ☐ |
| 5 | Write DTO | `{SolutionName}.Models/{Entity}/{Entity}CreateDto.cs` | ☐ |
| 6 | Service interface | `{SolutionName}.Services/I{Entity}Service.cs` | ☐ |
| 7 | Service implementation | `{SolutionName}.Services.Components/{Entity}Service.cs` | ☐ |
| 8 | Validator | `{SolutionName}.Services.Components/{Entity}Validator.cs` | ☐ |
| 9 | DI registration | `{SolutionName}.Api/ServiceRegistration.cs` | ☐ |
| 10 | Endpoint class | `{SolutionName}.Api/Endpoints/{Entity}/{Entity}Endpoints.cs` | ☐ |
| 11 | MapGroup call | `{SolutionName}.Api/Program.cs` | ☐ |
| 12 | TypeScript model | `client/src/app/models/{entity}/{entity}.model.ts` | ☐ |
| 13 | Angular service | `client/src/app/service/{entity}.service.ts` | ☐ |
| 14 | Angular component(s) | `client/src/app/{feature}/` | ☐ |

---

## 6. Build & Test Commands

### Server

```
cd server/{SolutionName}.Api
dotnet restore && dotnet build
dotnet run --project {SolutionName}.Api/{SolutionName}.Api.csproj
dotnet test
```

### Client

```
cd client
npm install
npm start          # dev server → http://localhost:4200
npm run build      # production (--base-href /client/)
npm test           # Karma + Jasmine
```
